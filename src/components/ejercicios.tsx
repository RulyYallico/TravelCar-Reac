interface IBoton {
    texto : string;
    color : string;
    onBtnClick: () => void;
}

export function BotonTravel() {

    // const btnColor  = !props.color ? "#fff" : props.color;

    return <button>Enviando Boton</button>

}